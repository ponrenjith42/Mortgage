spring:
  application:
    name: mortgage
  cache:
    type: none
  profiles:
    default: h2

management:
  endpoints:
    web:
      exposure:
        include: health,info,env

server:
  port: 8080
