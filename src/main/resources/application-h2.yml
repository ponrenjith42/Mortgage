spring:
  config:
    activate:
      on-profile: h2
  datasource:
    url: jdbc:h2:mem:mortgage-db
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true